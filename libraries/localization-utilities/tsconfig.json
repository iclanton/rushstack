{
  "extends": "./node_modules/@rushstack/heft-node-rig/profiles/default/tsconfig-base.json",
  "compilerOptions": {
    "target": "ES2019",
    "types": ["heft-jest", "node"],

    // This project is designed to be webpacked, so produce esnext by default. CommonJS is configured with config/typescript.json
    "module": "esnext",
    "moduleResolution": "node"
  }
}
